// Generated by CoffeeScript 1.6.3
'use strict';
$(function() {
  var $body, $document, $wrapper, app, navBarHeight, on_scroll, scrollTop;
  $body = $('body');
  $document = $(document);
  $wrapper = $('#wrapper');
  navBarHeight = $('.navbar').first().height();
  scrollTop = 0;
  on_scroll = function() {
    var snap, stop,
      _this = this;
    stop = $document.scrollTop();
    if (stop < navBarHeight) {
      $wrapper.click(function(e) {
        $document.scrollTop(navBarHeight);
        return e.preventDefault();
      });
    } else {
      $wrapper.off();
    }
    if (scrollTop > stop && stop !== 0) {
      snap = 0;
    } else if (scrollTop < stop && stop !== navBarHeight) {
      snap = navBarHeight;
    }
    $.doTimeout('scrolling', 150, function() {
      return $document.scrollTop(snap);
    });
    return scrollTop = stop;
  };
  $document.scrollTop(navBarHeight);
  $document.on('scroll', $body, on_scroll);
  return app = new App(function($el, clear) {
    if (clear == null) {
      clear = false;
    }
    return null;
  });
});
